#pragma once
#include <vector>
typedef unsigned long long ULL;

void mergesort(std::vector<ULL>&, std::vector<ULL>&, size_t, size_t,bool);
void merge_sort(std::vector<ULL>&, size_t, size_t);
void merge_sort_optimized(std::vector<ULL>&, size_t, size_t);